<xml>
    <lang type="c++">
        <includes>
            <file name="libpq-fe.h" />
            <file name="arpa/inet.h" />
        </includes>
        <global_functions>
            <function>
                <![CDATA[
                #if !defined(PQ_RESULT_FORMAT_TEXT) || !defined(PQ_RESULT_FORMAT_BINARY)
                #define PQ_RESULT_FORMAT_TEXT 0
                #define PQ_RESULT_FORMAT_BINARY 1
                #endif

                class PQResult
                {
                private:
                    PGresult    *m_res;

                public:
                    PQResult(): m_res(0) {}
                    PQResult(PGresult *_res): m_res(_res) {}
                    ~PQResult() { if (m_res) PQclear(m_res); }

                    const PGresult* get() const { return m_res; }

                    void operator=(PGresult *_res)
                    {
                        if (m_res)
                            PQclear(m_res);

                        m_res = _res;
                    }
                };

                #define CHECK_ERROR(PQRESULT, CONN, STATUS, STR) ASSERT_MSG(PQresultStatus(PQRESULT.get()) == STATUS, STR << PQerrorMessage(CONN));
                ]]>
            </function>
        </global_functions>
        <types>
            <connection>
                <type value="PGconn*"/>
                <null value="NULL"/>
            </connection>
            <statement>
                <type value="PQResult"/>
                <null value="PQResult()"/>
            </statement>
        </types>
        <connect>
            <![CDATA[
            m_conn = PQconnectdbParams(keywords.data(), values.data(), 0);
            {
                ConnStatusType status = PQstatus(m_conn);
                ASSERT_MSG(status == CONNECTION_OK, "PostgreSQL: " << PQerrorMessage(m_conn));
            }
            ]]>
        </connect>
        <disconnect>
            PQfinish(m_conn);
        </disconnect>
        <select>
            <create>
            </create>
            <destroy>
            </destroy>
            <prepare>
                <![CDATA[
                m_selectStmt = PQprepare(m_conn, "%SELECT_NAME%", s_selectSQL, s_selectSQL_len, 0);
                CHECK_ERROR(m_selectStmt, m_conn, PGRES_COMMAND_OK, "PostgreSQL: Error peraring:\n" << s_selectSQL << "\n" << PQerrorMessage(m_conn));
                ]]>
            </prepare>
            <fetch>
                <![CDATA[
                if(++m_rowNum < m_rowCount)
                {
                    m_currentRow.reset( new _row_type( this ));
                    return true;
                }
                else
                {
                    m_currentRow.reset();
                    return false;
                }
                ]]>
            </fetch>
            <execute>
                <![CDATA[
                m_selectStmt = PQexecPrepared(m_conn, "%SELECT_NAME%", 1, paramValues, paramLengths, paramFormats, PQ_RESULT_FORMAT_BINARY);
                CHECK_ERROR(m_selectStmt, m_conn, PGRES_TUPLES_OK, "PostgreSQL: Error executing:\n" << s_selectSQL << "\n" << PQerrorMessage(m_conn));
                m_rowCount = PQntuples(m_selectStmt.get());
                ]]>
            </execute>
            <reset>
                m_selectStmt = PQResult();
                m_rowNum = -1;
                m_rowCount = -1;
            </reset>
        </select>

        <insert>
            <create>
            </create>
            <destroy>
            </destroy>
            <prepare>
            </prepare>
            <execute>
            </execute>
            <reset>
            </reset>
        </insert>

        <update>
            <create>
            </create>
            <destroy>
            </destroy>
            <prepare>
            </prepare>
            <execute>
            </execute>
            <reset>
            </reset>
        </update>

        <delete>
            <create>
            </create>
            <destroy>
            </destroy>
            <prepare>
            </prepare>
            <execute>
            </execute>
            <reset>
            </reset>
        </delete>

    </lang>
</xml>
